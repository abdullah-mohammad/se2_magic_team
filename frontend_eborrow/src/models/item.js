export default class Item {
    constructor(title, description, picture, available,user) {
        this.title = title;
        this.description = description;
        this.picture = picture;
        this.available = available;
        this.user = user;
    }
}
